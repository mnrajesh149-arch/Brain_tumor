This project focuses on developing a deep learning-based system for detecting brain tumors from medical brain images. The project involves collecting and preprocessing the image dataset, preparing the images for model training, building a suitable neural-network model, training the model on labeled images, and evaluating its performance. The trained model can then be used to classify new brain images according to the classes present in the dataset
